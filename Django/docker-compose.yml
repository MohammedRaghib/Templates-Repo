services:
  web:
    build: .
    user: "1000:1000"
    volumes:
      - .:/app
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8000:8000"
    stdin_open: true
    tty: true